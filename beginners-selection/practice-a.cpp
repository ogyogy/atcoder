#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main(int argc, char const *argv[])
{
    ll a, b, c;
    string s;
    cin >> a;
    cin >> b >> c;
    cin >> s;
    cout << a + b + c << " " << s << endl;
    return 0;
}
